import { Component } from '@angular/core';

/**
 * RegisterTemplateComponent — Template
 *
 * Layout centrado para la pantalla de registro.
 * Sigue el mismo patrón que LoginTemplateComponent.
 */
@Component({
  selector: 'app-register-template',
  standalone: false,
  templateUrl: './register-template.html',
  styleUrls: ['./register-template.scss']
})
export class RegisterTemplateComponent {}